{% extends 'base.html.twig' %}

{% block title %}Birthday{% endblock %}

{% block body %}
    <h1>Birthday</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id : </th>
                <td>{{ birthday.id }}</td>
            </tr>
            <tr>
                <th>Nom : </th>
                <td>{{ birthday.nom }}</td>
            </tr>
            <tr>
                <th>Prenom : </th>
                <td>{{ birthday.prenom }}</td>
            </tr>
            <tr>
                <th>Date d'anniversaire :</th>
                <td>{{ birthday.date ? birthday.date|date('d/m/Y') : '' }}</td>
            </tr>
            <tr>
                <th>Action : </th>
                <td><a href="{{ path('app_birthday_edit', {'id': birthday.id}) }}">Modifier</a></td>
            </tr>
        </tbody>
    </table>

    <button onclick="window.location='{{ path('app_birthday_index') }}'">Retour à la liste</button>

    {{ include('birthday/_delete_form.html.twig') }}
{% endblock %}
